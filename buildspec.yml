version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto21
  pre_build:
    commands:
      - echo Pre-build started
      - chmod +x mvnw
  build:
    commands:
      - echo Building the project...
      - ./mvnw package -DskipTests
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - target/*.jar
